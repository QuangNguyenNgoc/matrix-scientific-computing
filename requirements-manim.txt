# =============================================
# requirements-manim.txt — WSL/Linux (Python 3.11)
# Dành cho: người render Manim (WSL)
# Cài đặt: pip install -r requirements-manim.txt
# =============================================

# --- Kế thừa toàn bộ từ requirements.txt ---
-r requirements.txt

# --- Manim render engine ---
manim==0.20.1

# --- LaTeX vẫn cần cài riêng ngoài pip ---
# sudo apt install texlive texlive-latex-extra